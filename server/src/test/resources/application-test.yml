spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

platform:
  security:
    secure-api: false
  iot:
    provider: mock
    fallback-to-mock: true
    kepserver:
      enabled: false
